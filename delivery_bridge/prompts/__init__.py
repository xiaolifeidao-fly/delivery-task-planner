"""发给执行器的提示词。

面板掌握的是结构化事实（任务、阶段、文档路径、分支、引用），执行器需要的是
一段能照着干活的话。这一层只做这个翻译，不碰会话状态，也不发请求。

  common.py       各阶段共用的上下文行与规则行，以及技能点名
  task.py         任务级：动作执行、成品测试用例、微调
  planning.py     需求拆解与其后续轮次，含过程摘要临时文档
  conversation.py 自由聊天、会话标题、需求起名
  requirement.py  需求级：总体测试、评审、原型
  environment.py  预设环境安装
"""
